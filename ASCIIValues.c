#include<stdio.h>
int main (){
    char ch;
    printf("enter th letter for ascii value:");
    scanf("%c",&ch);
    printf("the ASCII value of the given letter %c is %d\n",ch,ch);
    
    return 0;
}